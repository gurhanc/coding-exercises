def insertNodeAtTail(head, data): 
    if head is None: 
        return SinglyLinkedListNode(data) 
    else: 
        node = head 
        while node.next: 
            node = node.next 
        new_node = SinglyLinkedListNode(data) 
        node.next = new_node 
        return head   
