import re
n = int(input())
pattern = r"^[789][0-9]+$"
for i in range(n):
    number = input()
    if len(number) == 10 and re.search(pattern,number):
        print("YES")
    else:
        print("NO")
